name: "CodeQL"

on:
  push:
    branches: [main, protected]
    paths-ignore:
      - ".github/**"
      - "pictures/**"
      - "**/*.md"
  pull_request:
    branches: [main, protected]
    paths-ignore:
      - ".github/**"
      - "pictures/**"
      - "**/*.md"
